1.	Extension methods are static methods. Therefore it should be declared inside static class.
2.	The first parameter of the extension method defines the type of the method, on which it will get called.
	preciding with 'this' keyword